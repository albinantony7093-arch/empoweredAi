import 'package:flutter/material.dart';

class CalmCorner extends StatelessWidget {
  const CalmCorner({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.white);
  }
}
